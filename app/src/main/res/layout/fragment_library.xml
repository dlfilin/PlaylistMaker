<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white_black"
    android:orientation="vertical"
    tools:context=".ui.library.LibraryFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:paddingHorizontal="@dimen/space_16"
        android:orientation="horizontal">

        <TextView
            style="@style/TopTitleStyle"
            android:text="@string/library"
            android:textColor="@color/black_white" />

    </LinearLayout>

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/size_48"
        android:background="@color/white_black"
        app:tabIndicator="@drawable/tab_indicator"
        app:tabIndicatorColor="@color/black_white"
        app:tabMode="fixed"
        app:tabPaddingEnd="@dimen/space_16"
        app:tabPaddingStart="@dimen/space_16"
        app:tabTextAppearance="@style/ys_medium_14"
        app:tabTextColor="@color/black_white" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>